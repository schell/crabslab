[workspace]
members = [
    "crates/crabslab",
    "crates/crabslab-derive",
    "crates/example",
]

# 'shaders' contains a test spirv shader to measure filesize when using crabslab
exclude = ["./shaders"]

resolver = "2"

[workspace.dependencies]
glam = { path = "../glam-rs", version = "0.27", default-features = false }
